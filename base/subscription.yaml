apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: web-terminal
  namespace: openshift-operators
spec:
  channel: fast # Use the desired channel (check the OperatorHub for options)
  installPlanApproval: Automatic
  name: web-terminal
  source: redhat-operators # Source catalog
  sourceNamespace: openshift-marketplace
